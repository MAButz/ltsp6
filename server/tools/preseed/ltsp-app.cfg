# Preseed for an LTSP application server ("ltsp-app1", "ltsp-app2", ...).
#
# These hosts run the actual user sessions: xrdp accepts the RDP connections
# that HAProxy on lb1/lb2 distributes to them, and xfce4 is the desktop those
# sessions get. They build and serve no images at all - that is ltsp-root's
# job - and they hold no user data either, since home directories come from
# the file server over the network.
#
# One of these runs per Proxmox node so the load balancers have more than one
# backend to distribute across. The hostname therefore has to be set per
# instance: build the ISO once per host, editing netcfg/get_hostname below,
# or override it on the installer command line with hostname=<name>.

d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string ltsp-app1
d-i netcfg/get_domain string ltsplab.local
d-i netcfg/wireless_wep string

d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

d-i clock-setup/utc boolean true
d-i time/zone string Europe/Berlin
# Kerberos will not issue tickets if the clock drifts more than a few minutes
# from the domain controller's, so time sync is not optional on a domain
# member.
d-i clock-setup/ntp boolean true

d-i partman-auto/disk string /dev/sda
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman-efi/non_efi_system boolean true

d-i passwd/root-login boolean true
d-i passwd/root-password password proxmoxlab
d-i passwd/root-password-again password proxmoxlab
d-i passwd/make-user boolean false

d-i base-installer/kernel/image string linux-image-amd64
tasksel tasksel/first multiselect standard, ssh-server

# xrdp/xorgxrdp accept the RDP sessions, xfce4 is the desktop inside them,
# dbus-x11 is what lets an Xsession start under xrdp at all. xscreensaver
# locks the screen (light-locker, which xfce4 would otherwise pull in, cannot
# lock an xrdp session - it has no logind seat to lock). The
# krb5-user/sssd/realmd set joins this host to the Samba AD domain so logins
# authenticate against the DC, and nfs-common mounts the home directories
# from the file server. ethtool is needed by ltsp-nic-offload.service, which
# setup-ltsp-app installs - without the binary that unit just fails and the
# nested-virtualisation checksum bug comes back.
d-i pkgsel/include string sudo xrdp xorgxrdp xfce4 xfce4-terminal dbus-x11 x11-xserver-utils xscreensaver realmd sssd sssd-tools libnss-sss libpam-sss adcli krb5-user samba-common-bin nfs-common ethtool chrony
d-i pkgsel/upgrade select none
popularity-contest popularity-contest/participate boolean false

d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

d-i finish-install/reboot_in_progress note

# krb5-config would otherwise stop and ask for the realm interactively.
krb5-config krb5-config/default_realm string LTSPLAB.LOCAL
krb5-config krb5-config/add_servers_realm string LTSPLAB.LOCAL

# Power off instead of rebooting when the install is done.
#
# QEMU fixes the boot order when its process starts, so a guest-initiated
# reboot at the end of the install comes straight back to the still-attached
# installer ISO and installs again, forever. Powering off breaks that loop:
# detach the ISO, then start the VM into the installed system.
d-i debian-installer/exit/poweroff boolean true

d-i preseed/late_command string in-target sh -c "\
sed -i -E 's/^#?PermitRootLogin.*/PermitRootLogin yes/' /etc/ssh/sshd_config; \
grep -q '^PermitRootLogin yes' /etc/ssh/sshd_config || echo 'PermitRootLogin yes' >> /etc/ssh/sshd_config; \
printf 'net.ipv6.conf.all.disable_ipv6 = 1\nnet.ipv6.conf.default.disable_ipv6 = 1\n' > /etc/sysctl.d/99-disable-ipv6.conf"
